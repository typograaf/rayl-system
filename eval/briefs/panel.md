Build a control panel for a tool that generates repeating plate patterns.

It needs: an aspect ratio chooser with five options, a subject mode with three,
sliders for count, tilt and duration, a row of buttons to export and to reset,
and somewhere the current output size is displayed as a value that updates.

Single column, no more than a screen tall.
