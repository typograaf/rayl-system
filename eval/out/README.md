# Answers

One `<brief>.html` per brief, saved exactly as it came back — not tidied, not
corrected, not re-prompted until it passed.

They are committed on purpose. A run is the record of what the system produced
on a given day, and the diff between two runs is the only evidence anybody has
that a change to the rules made things better rather than different.
